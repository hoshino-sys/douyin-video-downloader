## 更新内容（5.9.2）

- **界面更换字体**：内置思源黑体（Noto Sans SC），中文显示更统一清晰
- **数据目录拆分**：`UserData` 只放配置/Cookie/数据库，新增 `Downloads` 专门存放下载的视频等媒体文件，旧数据首次启动自动迁移
- **按平台分类保存**（默认开启，设置可关）：下载文件自动归入 抖音/TikTok/B站/YouTube 子文件夹
- 修复缓存目录大小写混用问题；打包成功后自动清理旧版本产物

## 安装 / 更新

1. 下载下方 `yexing-video-downloader_v5.9.2.zip`
2. 解压到任意目录（覆盖旧目录前请先退出正在运行的程序）
3. 双击 `夜星视频下载器.exe`；如从旧版本升级，原 `Volume` 文件夹内的配置与下载记录会自动迁移到新的 `UserData` / `Downloads` 目录

> 更新包内已内置 ffmpeg 与 JS 运行时，无需安装任何环境。经 QQ/微信中转可能剥离/隔离 exe，请重新完整解压。

---

<details>
<summary>5.9.1 及更早（历史）</summary>

### 5.9.1
- 项目结构整理：数据库文件更名为 `yexing-video-downloader.db`（自动迁移）；精简无用文件；修正依赖声明

### 5.9.0
- **Cookie 状态一目了然**：顶部状态条分平台显示四个平台的导入与登录状态
- **实时下载进度条**：百分比 · 已下载/总量 · 速度 · 剩余时间
- **任务卡片改版**：大字显示视频标题，彩色徽标区分来源平台
- **应用内检查更新**：设置 → 关于 → 检查更新，可直接在应用内下载更新包
- 内置 quickjs JS 运行时：无 Node.js 的电脑也能解析 YouTube（5.8.2 起）

</details>
